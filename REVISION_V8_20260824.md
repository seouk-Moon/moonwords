# MoonWords Revision V8 · 2026-08-24

## 목표
기존 학습/단어장/퀴즈/프로필/폴더 기능은 유지하면서 본문 듣기 조작성과 로그인 유지 방식을 개선합니다.

## 변경 사항

### 1. 본문 듣기 하단 컨트롤
- 기존 상단 `전체 듣기 / 일시정지 / 계속 듣기 / 정지` 기능을 그대로 유지했습니다.
- 상단 듣기 컨트롤이 화면 위로 사라지면 화면 하단에 동일한 미니 컨트롤이 표시됩니다.
- 전체 듣기 중 현재 문장 번호와 `재생 중 / 일시정지` 상태가 하단 컨트롤에도 표시됩니다.
- 상단 컨트롤이 다시 보이면 하단 컨트롤은 자동으로 사라져 중복 UI를 줄였습니다.

### 2. 개별 문장 듣기 상태 표시
- 문장별 `◉ 듣기`를 누르면 즉시 `■ 듣기 중`으로 바뀌고 파란색 활성 상태가 표시됩니다.
- 재생 중인 문장도 전체 듣기와 동일한 강조 배경을 사용합니다.
- 같은 문장의 활성 버튼을 다시 누르면 재생이 중지됩니다.

### 3. 자동 로그인 선택
- 로그인 화면에 `자동 로그인` 체크박스를 추가했습니다.
- 체크: Supabase 세션을 `localStorage`에 저장해 브라우저를 닫았다가 열어도 로그인 상태를 복원합니다.
- 체크 해제: 세션을 `sessionStorage`에만 저장해 현재 탭 세션 동안만 유지합니다.
- 기존 Supabase `autoRefreshToken` 및 세션 복구 흐름은 유지합니다.
- 비밀번호 자체는 브라우저 저장소에 저장하지 않습니다.

## 구조
- 듣기 UI: `src/features/study/ListeningControls.tsx`
- 자동 로그인 저장소: `src/lib/auth-storage.ts`
- 기존 큰 파일에 기능을 중복 작성하지 않고 별도 모듈로 분리했습니다.

## 검증
- 변경 TypeScript/TSX 파일 syntax transpile 검사 통과
- `git diff --check` 통과
- 자동 로그인 저장소의 local/session 전환 및 삭제 동작 단위 확인
- 패키징 환경에서 전체 `npm ci`는 외부 의존성 캐시 부족/설치 제한으로 완료되지 않아, 배포 시 GitHub Actions의 `build:pages` 결과를 최종 확인해야 합니다.
