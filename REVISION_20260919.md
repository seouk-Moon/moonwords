# MoonWords Revision · 2026-09-19

## 반영 내용

- AI 본문 이해 문제 추가 상한을 5개에서 10개로 변경했습니다.
- 추가 문제 생성 시 전체 본문 분석을 다시 만들지 않고 문제 데이터만 요청하도록 분리했습니다.
- 본문 학습의 `이해 체크` UI와 이해 완료 표시를 제거하고 `어려운 문장 체크`만 유지했습니다.
- 전체/개별 듣기 버튼을 재생·일시정지 SVG 아이콘으로 바꾸고, 개별 문장도 실제 일시정지/이어 듣기가 가능하게 했습니다.
- 본문 학습 화면에서 제목을 편집하고 Supabase `documents.title`에 저장할 수 있게 했습니다.
- 선택한 폴더를 앞/뒤로 이동하고 사용자별 순서를 Supabase에 저장할 수 있게 했습니다.
- Gemini 429/503/5xx에 서버 측 지수 백오프·지터 재시도와 안정 모델 fallback을 추가했습니다.
- 압축 해제 상태에서도 `npm test`가 실행되도록 셸 스크립트 호출 방식을 보완했습니다.
- 같은 GitHub Pages 안에 독립형 `pdf-extractor.html`을 추가하고 관련 코드를 `src/pdf-extractor/`에 분리했습니다.
- PDF는 서버에 업로드하지 않고 브라우저에서 텍스트를 추출하며, 복사·TXT 저장·Moonwords 본문 입력 연동을 지원합니다.
- PDF 추출기는 새 Supabase 리소스를 사용하지 않아 기존 프로젝트 제한과 배포 구조에 영향을 주지 않습니다.
- 글자 정보가 없거나 깨진 저화질·스캔 PDF는 페이지 확대와 대비 보정 후 브라우저 영어 OCR로 자동 재시도합니다.
- 정상 추출 결과가 마음에 들지 않을 때 사용할 수 있는 `OCR로 다시 읽기`도 추가했습니다.
- 앱 오른쪽 아래에 채팅형 `문제 해결 챗봇`을 추가했습니다. PDF/OCR, Gemini 503, 문장 수, 제목·폴더, 퀴즈와 듣기 사용법을 안내하며 외부 API를 호출하지 않습니다.
- 상단 문장 수와 실제 표시 개수가 달라지거나 번호가 29처럼 중간에서 끝나는 문제를 수정했습니다.
- 기존 문서는 저장된 문장 ID를 건드리지 않고 화면 번호와 문단 배치만 복구해 단어장·어려운 문장 기록을 유지합니다.
- 새 분석 결과는 프런트엔드와 Edge Function에서 문장 ID를 1부터 연속으로 정리하고 잘못된 문단 연결을 자동 보정합니다.

## 운영 반영 시 필수 작업

```bash
npx supabase link --project-ref YOUR_PROJECT_REF
npx supabase db push
npx supabase secrets set GEMINI_MODEL=gemini-3.8-flash
npx supabase secrets set GEMINI_FALLBACK_MODELS=gemini-3.7-flash,gemini-3.5-flash-lite
npx supabase functions deploy process-document
```

CLI 대신 Supabase SQL Editor를 사용한다면 `supabase/migrations/202609190001_folder_order.sql`을 한 번 실행합니다. 프런트엔드만 GitHub Pages에 배포하면 폴더 순서 저장과 Gemini 안정화 코드는 활성화되지 않으므로 Edge Function 재배포가 필요합니다.

## 검증

- `npm test`: 통과
- `npm run build:pages`: 통과
- 변경된 프런트엔드 파일 ESLint: 통과
- Edge Function TypeScript 구문 변환 검사: 통과
- `git diff --check`: 통과
