# MoonWords v3 수정 사항 — 2026-08-23

기존 기능은 삭제하지 않고 아래 기능/UX만 확장했습니다.

## 반영 내용

1. 어순 배열에서 긴 문장의 일부만 출제될 경우
   - 앞 문장 / 다음 문장뿐 아니라
   - 같은 현재 문장에서 출제 구간 앞부분 / 뒷부분도 문맥으로 표시합니다.
2. `어려움 체크` 문구를 `어려운 문장 체크`로 변경했습니다.
3. 새로 불러오는 원본의 밑줄 표시
   - DOCX 밑줄 run을 감지해 분석된 해당 문장 앞에 `★`를 표시합니다.
   - PDF의 실제 Underline annotation도 가능한 경우 텍스트 위치와 매칭해 `★`를 표시합니다.
   - PDF에 단순 선 그림으로 그어진 밑줄은 PDF 구조상 일반 그래픽과 구분할 수 없어 감지되지 않을 수 있습니다.
4. 퀴즈 화면에 들어가자마자 문제가 노출되지 않습니다.
   - 종류와 옵션을 고른 뒤 `N문제 시작`을 눌러야 문제가 나타납니다.
   - 진행 중에는 `설정 변경`으로 다시 설정 화면으로 돌아갈 수 있습니다.
5. 본문 폴더 기능 추가
   - 폴더 생성 / 이름 변경 / 삭제
   - 폴더별 본문 필터
   - 각 본문을 폴더로 이동
   - 폴더 삭제 시 본문은 삭제하지 않고 `미분류`로 이동
6. 단어 뜻 팝업은 바깥을 클릭해도 닫힙니다.
7. AI 단어 뜻 조회 중 선택 단어를 즉시 하이라이트합니다.
   - 팝업을 닫고 본문을 계속 읽어도 조회 요청은 계속됩니다.
   - 완료된 뜻은 캐시에 남아 같은 단어를 다시 선택하면 바로 표시됩니다.
8. 상단 내비게이션 역할 분리
   - `내 본문`은 특정 본문 학습 탭에서 제거했습니다.
   - 특정 본문을 열었을 때는 `본문 학습 / 단어장 / 퀴즈`만 같은 그룹으로 표시합니다.
   - `내 본문`은 별도의 뒤로가기 버튼/로고로 이동합니다.
   - 모바일 하단 탭도 현재 본문에서는 세 학습 메뉴만 표시합니다.

## Supabase에서 반드시 할 일 — 폴더 기능

폴더 기능을 사용하려면 새 migration을 한 번 적용해야 합니다.

파일:

- `supabase/migrations/202608230002_document_folders.sql`
- 또는 저장소 루트의 `SUPABASE_FOLDER_SETUP.sql`

Supabase Dashboard → SQL Editor에서 `SUPABASE_FOLDER_SETUP.sql` 내용을 실행해도 됩니다.

이 migration은 기존 본문을 삭제하거나 변경하지 않습니다. 기존 본문의 `folder_id`는 `NULL`이라 모두 `미분류`로 보입니다.
폴더 삭제 시 외래키가 `ON DELETE SET NULL`이므로 폴더 안 본문도 유지됩니다.

## Edge Function

`supabase/functions/process-document/index.ts`는 v2와 동일합니다.
따라서 이번 수정 때문에 Edge Function을 다시 배포할 필요는 없습니다.

## 밑줄 표시 범위

밑줄 정보는 기존에 이미 분석해서 DB에 저장된 본문에는 없기 때문에 **새로 업로드하는 본문부터** 자동 `★` 표시가 적용됩니다.
기존 본문을 표시하려면 원본 파일을 다시 분석해야 합니다.

## 적용

기존 Git 저장소 폴더에 ZIP 내용을 덮어쓴 뒤, 폴더 기능을 쓸 경우 위 SQL migration을 적용하세요.
그 다음 평소처럼 빌드/배포하면 됩니다.
