# MoonWords V7 revision — 2026-08-23

## 요청 반영

1. 하단 `서비스` 메뉴를 실제 버튼으로 변경하고 서비스 안내 페이지를 추가했습니다. 하단 보조 링크에도 `서비스`를 추가했습니다.
2. 본문 학습 화면에 `원문 보기`를 추가했습니다. 기존 문장별 번역/단어/듣기/체크 기능은 그대로 유지되며, 우측 원문 패널에서 업로드 당시 원문 텍스트를 확인할 수 있습니다. 원본 파일이 저장된 문서는 `원본 파일 열기`도 사용할 수 있습니다.
3. 회원가입 시 본명, 닉네임, 성별을 설정할 수 있습니다. 기존 이메일/비밀번호 로그인은 그대로 유지됩니다.
4. 로그인 후 프로필 화면에서 프로필 사진을 등록/변경할 수 있습니다. 프로필 사진 Storage 설정은 `SUPABASE_PROFILE_SETUP.sql`을 사용합니다.
5. 첫 `내 본문` 화면에서 학습 성장 대시보드를 제거하고, 상단 프로필 버튼으로 이동한 프로필 화면 안에서만 학습 현황을 확인하도록 변경했습니다.

## Supabase 추가 설정

프로필 사진을 사용하려면 Supabase SQL Editor에서 `SUPABASE_PROFILE_SETUP.sql`을 한 번 실행합니다. 이 SQL은 `avatars` Storage bucket과 사용자별 업로드 정책만 추가하며 기존 본문/단어/학습 기록 데이터는 삭제하거나 변경하지 않습니다.
